module.exports = {

  // the custom template for the generic classes
    generateScopedName: '[name]__[local]-[hash:base64:5]',
    mode: 'local',

  // rootDir: './common/page',    // if classname not match with user, use this option
    camelCase: true
};
